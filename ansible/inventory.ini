[mediaserver]
dhlab-iii.dhlab.unibas.ch ansible_user=rosenth

[mediaserver:vars]
ansible_become=true
# Sudo password is prompted at runtime via: ansible-playbook ... -K

# Optional: pin SSH identity file if needed
# ansible_ssh_private_key_file=~/.ssh/id_ed25519
