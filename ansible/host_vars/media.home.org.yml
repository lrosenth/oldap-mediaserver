# Test deployment target.
ansible_python_interpreter: /usr/bin/python3.14
ansible_become_exe: /usr/bin/sudo.ws
ansible_become_method: sudo
ansible_become_user: root
ansible_become_flags: "-H -S"

media_domain: media.home.org
# The API is hosted on a separate home VM. Use HTTP for this server-to-server
# call so the media containers do not need to trust the API Caddy instance's
# private certificate authority. Public browser traffic remains HTTPS.
oldap_api_url: http://api.home.org
# The home environment has the purpose-specific export secrets provisioned and
# is the acceptance target for the ZIP export worker before production rollout.
zip_export_worker_enabled: true
cors_origins: >-
  https://app.home.org,https://fasnacht.home.org,http://app.home.org,http://fasnacht.home.org

# media.home.org is not publicly resolvable, so Caddy must not request a
# public ACME certificate. The explicit http:// address keeps HTTP available;
# tls internal enables HTTPS with Caddy's local CA.
caddy_auto_https: disable_redirects
caddy_site_addresses:
  - http://media.home.org
  - https://media.home.org
caddy_tls_internal: true
