.git
.gitignore
.idea
.venv
__pycache__
*.py[cod]

# Local/runtime data must never be sent as Docker build context.
data
caddy-conf
caddy-data
tars
*.tar
*.tar.gz
*.tgz
*.zip

# Keep the mediaserver build context small. The imageserver has its own build
# context; retired licensed artifacts remain excluded as a safety measure.
imageserver/cantaloupe-5.0.7
kakadu-base/v8_6_2-01727L
